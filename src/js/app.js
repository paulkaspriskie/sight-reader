import React from 'react';
import ReactDOM from 'react-dom';

const rootElement = document.getElementById('app');

ReactDOM.render(
  <div id="app">
    <h1>sight-reader</h1>
  </div>,
  rootElement
);
